import React from 'react'
import { Button } from '../ui/button'

const CreateWallet = () => {
  return (
    <div>
      <Button className='rounded-[10px] font-semibold'>+ Create Wallet</Button>
    </div>
  )
}

export default CreateWallet